/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * JJ-2: dual-band roadmap contract — phases and milestones as two related, synchronizable lists.
 */
export type Timeline01SlideSpec = SlideSpecBase & {
  /**
   * Contract version for codegen and forward compatibility (section 32, AT-6).
   */
  schema_version: "1.0";
  /**
   * Stable slide identifier within a presentation version (section 14 example, section 22 API).
   */
  slideId?: string;
  layoutId: "TIMELINE_01";
  /**
   * Small eyebrow label above the title, e.g. ARCHITECTURE (section 14).
   */
  sectionLabel?: string;
  /**
   * Primary slide heading.
   */
  title: string;
  /**
   * Secondary heading under the title when the layout supports it (section 14).
   */
  subtitle?: string;
  sourceChapterIds: SourceChapterIds;
  fieldProvenance?: FieldProvenance;
  /**
   * Upper-band timeline phases; item-count limits are defined separately in JJ-10.
   */
  phases: TimelinePhase[];
  /**
   * Lower-band milestones synchronized to phases via phaseId.
   */
  milestones: TimelineMilestone[];
};
/**
 * Mandatory traceability to FrameworkObject chapters (section 14).
 *
 * @minItems 1
 *
 * Items: FrameworkObject chapter_id (0-13). Used in sourceChapterIds (section 14).
 */
export type SourceChapterIds = [string, ...string[]];
/**
 * Optional shared field-level traceability. Layout groups enforce complete coverage in BT-14 / JJ-9 / MS-11.
 *
 * @minItems 1
 */
export type FieldProvenance = [FieldProvenanceEntry, ...FieldProvenanceEntry[]];

/**
 * AT-3: shared fields every layout-specific SlideSpec extends. Technical plan section 14.
 */
export interface SlideSpecBase {
  /**
   * Contract version for codegen and forward compatibility (section 32, AT-6).
   */
  schema_version: "1.0";
  /**
   * Stable slide identifier within a presentation version (section 14 example, section 22 API).
   */
  slideId?: string;
  /**
   * Must match a layout in layout_registry.json (section 13.1).
   */
  layoutId:
    | "COVER_01"
    | "EXECUTIVE_SUMMARY_01"
    | "CONTEXT_01"
    | "PROBLEM_SOLUTION_01"
    | "SCOPE_01"
    | "REQUIREMENTS_MATRIX_01"
    | "PROCESS_FLOW_01"
    | "TIMELINE_01"
    | "MILESTONES_01"
    | "TEAM_FTE_01"
    | "ARCHITECTURE_01"
    | "COMPLIANCE_01"
    | "SUCCESS_METRICS_01"
    | "OPEN_QUESTIONS_01"
    | "NEXT_STEPS_01";
  /**
   * Small eyebrow label above the title, e.g. ARCHITECTURE (section 14).
   */
  sectionLabel?: string;
  /**
   * Primary slide heading.
   */
  title: string;
  /**
   * Secondary heading under the title when the layout supports it (section 14).
   */
  subtitle?: string;
  sourceChapterIds: SourceChapterIds;
  fieldProvenance?: FieldProvenance;
  [k: string]: unknown;
}
export interface FieldProvenanceEntry {
  /**
   * Canonical dotted/array field path aligned with AT-8, e.g. problem.description or items[0].title.
   */
  path: string;
  /**
   * Framework chapters that support this populated content field.
   *
   * @minItems 1
   *
   * Items: FrameworkObject chapter_id (0-13). Used in sourceChapterIds (section 14).
   */
  sourceChapterIds: [string, ...string[]];
}
export interface TimelinePhase {
  /**
   * Stable phase id referenced by milestones[].phaseId.
   */
  id: string;
  name: string;
  description: string;
}
export interface TimelineMilestone {
  id: string;
  name: string;
  /**
   * Must match a phases[].id so the two bands stay aligned.
   */
  phaseId: string;
  /**
   * Optional relative or calendar label, e.g. Week 2 or 2026-09.
   */
  date?: string;
  description?: string;
}
